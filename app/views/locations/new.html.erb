<h2>Create Location</h2>
<%= form_for @location do |f| %>

<%= f.label :city %>
<%= f.text_field :city, required: true %>

<%= f.label :state %>
<%= f.text_field :state, required: true %>

<%= f.label :country %>
<%= f.text_field :country, required: true %>

<%= f.label :trip_id %>
<%= f.number_field :trip_id, :value => trip.id %>

<% @trip = current_user.trip.find(params[:trip_id]) %>

<%= f.submit %>
</div>

<% end %>
